"""Fallo deliberado para demostrar que el gate bloquea el merge.

Este archivo existe solo para que `ruff` falle y el check requerido salga rojo.
La rama se borra en cuanto se captura la evidencia.
"""

import os  # noqa: intencionalmente sin usar — es el fallo que buscamos
